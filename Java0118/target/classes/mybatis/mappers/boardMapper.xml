<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">


<!-- DAO랑 DB랑 작업할 때 필요한 SQL을 불러옵니다 -->

<mapper namespace="board">
	<select id="list" resultType="boardDTO">
		<!-- SQL쿼리문 작성 -->
		SELECT b_no, b_title, b_write, b_date, b_like
		FROM board ORDER BY b_no DESC limit 0, 10
	</select>

	<select id="detail" parameterType="boardDTO"
		resultType="boardDTO">
		<!-- SQL쿼리문 작성 -->
		SELECT b_no, b_title, b_content, b_write, b_date, b_like
		FROM board
		WHERE b_no=#{b_no}
	</select>

	<insert id="write" parameterType="boardDTO">
		<!-- SQL쿼리문 작성 -->

		INSERT INTO board
		SET
		b_title = #{b_title },
		b_content = #{b_content },
		b_write = #{b_write },
		b_date = now(),
		b_like = 1
	</insert>


</mapper>



